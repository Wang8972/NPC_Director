# 认知链实现与验证记录

日期：2026-09-13。

**通用后端实现与离线验收完成；Luna 的新功能 live 未达到 90% 门槛，旧场景 live 因预算停止而未跑完。**
功能默认保持关闭，通过 `NPC_DIRECTOR_COGNITION=true` 显式启用。不修改《余灯》、Unity 或
PerformanceDirective 1.0；未自动切换 Sol，未追加真实调用预算。

## 工程交付

- 五种可配置持续模式、Planner 内模式选择、来源与操作范围校验、真实完成后的模式提交。
- 按角色和实例隔离的经历、承诺、摘要、人物判断及关联；BM25 全量候选与关联召回，最多六条/1600 token。
- 事件时钟衰减、休眠召回、开放承诺/活动目标/配置保护、仅有效使用后的幂等强化。
- 无写世界工具的 MemoryConsolidator、带证据的新旧判断修订、过时结果拒绝及新观察合并。
- SQLite 增量存储与旧 scoped 迁移、维护任务租约/重试/预算、API worker 生命周期和受信 Python 查询接口。
- 短引用映射、无效模式提议保留旧模式、无效强化标注不授予权力；权限和事实边界仍由确定性代码处理。
- 计划与结构图已保存到用户指定的工作区文件；仓库内使用说明和结构图见 [COGNITION](COGNITION.md) 与 [架构](AGENT_ARCHITECTURE.md)。

## 离线结果

| 验证 | 实际结果 | 证据与边界 |
|---|---:|---|
| 通用 Python 回归 | **486 passed** | [最终 pytest 输出](eval/reports/cognition/pytest-final.log)，18.11 秒 |
| 最终代码 recorded | **138/138** | 旧34×3＋新增12×3，无 fallback；[报告](eval/reports/cognition/recorded-combined-final.json) |
| 《余灯》既有后端兼容检查 | **113 passed** | 未修改游戏代码；1 条既有依赖弃用提示，不代表 Unity 实机验收 |
| Ruff / diff 检查 | 通过 | 通用源码、新测试、评测与归档脚本 |

新增回归覆盖超256条旧记忆、查询不强化、重复完成、承诺保护、跨角色送达、跨实例隔离、
模式中断、过期来源、重启、并发 claim、预留与重试、晚到结算、新证据取代旧判断、非法模式和隐式操作。
最后补充了降级时空分析的完成路径和独立 Judge 的认知证据输入测试。

recorded 使用明确标识的模型 fixture，不证明自然度或自主学习质量。
最终 combined 报告的源码摘要与当前实现一致：
`b8f417b7effc57c1fafb1dc81ebfd75956ece81b5af09bbd313b0740656f98e6`。

## Luna 真实验证

生成、反思和独立 Judge 均使用 `gpt-5.6-luna`，SDK / `idealab_qwen`，调用超时60秒。
真实验证有试点、中断诊断、定向修复和正式批次，全部纳入同一持久账本。

| 批次 | 结果 | 说明 |
|---|---:|---|
| 初次试点 | 1/2 | 反思场景出现编号化措辞；保留失败 |
| 后续反思定向复测 | 1/1 | 来源及状态通过，自然度、人设5/5；不是全量结论 |
| 新增十二场景×3正式批次 | **27/36（75%）** | 结构33/36，2次fallback；未达90% |
| 新增正式自然度 / 人设 | **4.33 / 4.81** | 独立调用的模型评分，不是人工满意度 |
| 旧场景正式回归 | **已处理78/102** | 已处理部分48次严格通过；预算截断，不能作为完整质量结论 |

完整新增报告：[live-memory-release-36](eval/reports/cognition/live-memory-release-36.json)。
预算截断的旧回归：[live-v2-release-102](eval/reports/cognition/live-v2-release-102.json)。
旧回归的最后部分包括预算导致的未完成回合/缺失 Judge，不能把48/78与历史72/102直接比较。

未开始的旧场景共24次：existing_branch、authored_encounter、meaningful_short_quest、
merge_existing_quest、reject_unmotivated_quest、duplicate_content_receipt、casual_suggestion、
existing_route_interlude，各三次。它们的最终代码 recorded 已运行，但本轮 live 未完成。

## 预算与停止

保守累计 **9,954,926 / 10,000,000 token**，共2258次调用记录，8次最终未知用量按预留计账。
试点累计197,235 / 300,000 token；正式、诊断与后续复测合计9,757,691 token。
下一次请求的保守预留会超出上限，账本因此停止；没有把剩余45,074 token当作新增授权继续调用。

[账本摘要](eval/reports/cognition/token-ledger.json) 按阶段和角色保存计数。
原始持久账本位于 `artifacts/cognition/live-budget.db`；停止、重试和进程重启没有清零。
未配置供应商单价，不推算费用金额。

## 已修复问题与未解除的验收限制

1. 长散列抄写和冗余模式标签导致泛化拒答：增加受信短引用，模式提议失败保留旧模式；操作越权仍阻断。
2. 模式编排操作被误当成游戏动作：限制下游角色接收的认知配置，并检查编译后隐式节点。
3. 异步结果、重试及租约恢复：来源版本校验、执行者栅栏、每次新预留和先持久化用量保护。
4. 降级分析为 null 引发记忆触发器异常：最终代码已修复并补回归。该修复晚于上述 live 批次。
5. Judge 缺少认知证据：完整新增批次中，部分重启用例被错误要求用台词证明数据库恢复，
   部分回忆用例被要求保证现实位置。最终评判输入已补模式/记忆/维护证据并澄清故障演练语义。
   该修复在预算停止后完成，**未重新调用模型、未覆盖75%结果，也未宣称修复后已达到门槛**。

同时仍有真实的复杂规划、非法任务动作、格式、预算和回应遗漏问题。人工复核发现的评判问题
不自动转换为通过。关联场景的目标与提问也有需要进一步对齐之处，后续应使用版本化的新测试。
最终两项修复的离线结果包含在486项和138次recorded中；当前完整live证据对应修复前源码，
原始报告内的源码摘要和失败记录保持不变。详见 [ISSUE_LOG](ISSUE_LOG.md) NDC-01—05。

## 归档与复现

每份可读 JSON 旁有完整 `.json.gz`，解压后与原 artifacts 字节一致；SHA-256 见
[manifest](eval/reports/cognition/manifest.json)。中断批次显式标记，不充作完整验收。
原始试点、素材校准前快照、正式失败及预算截断记录均保留。

预算内的真实验证已经停止。后续若继续验证，需要明确新的预算安排，不能删除账本、换目录
或更名阶段绕过本次上限。建议先复核新 Judge 的人工样本，再验证记忆目标和剩余内容场景。
