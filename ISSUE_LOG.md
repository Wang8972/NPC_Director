# NPC Director 实施问题与复盘

本文件作为项目根目录的统一问题记录。本轮记录“事件驱动、多 NPC 协作与可审核叙事”升级
实际遇到的弯路，区分程序缺陷、模型行为和评测口径问题。每项记录现象、根因、修复、验证、
相关原理、预防方法和简短的面试表达；后续问题按编号追加。

本轮主要教训：离线契约正确、单拍质量不错，并不能证明整个 episode 能推进。应尽早用
真实模型跑少量有状态的完整生命周期，再扩展场景；不能等到所有模块写完才发现协议、预算或评判阶段不匹配。
各阶段曾同时改动模型、提示词和用例素材，下面的通过率仅记录诊断过程，不作单变量提升归因。

## NDV2-01：把离线通过当成真实对话已经可用

**现象与根因：** 最初 32 场景 ×3 的 recorded 运行通过，但 Luna 的真实试跑仅 8/32。
fixture 只能证明既定返回值能经过编排与状态链，无法证明模型正确路由、自然表达或在预算内完成协作。

**修复与验证：** 将结构检查与独立的场景质量评审分开，先复测失败场景，再运行完整套件；
fallback 和失败不从分母移除。保留 [8/32 试跑](eval/reports/director_v2/live-luna-pilot-32.json)、
[82/102 中间报告](eval/reports/director_v2/episodes-live-sol-34x3.json) 和后续完整报告。

**原理与预防：** 测试替身验证集成协议，真实模型验证行为分布；两者不能互相替代。
每条复杂新路径都应尽早跑真实的“生成—投递—完成—下一回合”。
**面试表达：** 我把结构成功率和场景成功率拆开，保留失败样本，避免用合法 JSON 代替效果验证。

## NDV2-02：用 Codex CLI 承载每个细粒度模型节点

**现象与根因：** CLI 每个节点都会带入大量额外上下文，观测到约 16K token 的额外负担，
既慢又容易耗尽 episode 预算。起初还只检查环境变量，没有复用用户已经配置的 provider 凭据。

**修复与验证：** 真实评测改用 SDK transport，显式管理调用超时、`max_retries=0`、
`store=False` 和本地重试；只在评测进程内映射已授权的 Codex 配置，保留显式环境配置优先级。
[传输回归](tests/test_model_transport_regressions.py) 验证了凭据不打印、不落入报告、不会发给不匹配端点。

**原理与预防：** 一次模型推理应由最小必要的 transport 承载；重试必须有一个统一的预算所有者。
**面试表达：** 我先分离编排问题和传输成本，再把多层隐式重试收敛到受控的节点重试。

## NDV2-03：认为原生 structured output 等于可靠的完整契约

**现象与根因：** 有的模型/网关忽略复杂 schema；部分嵌套 Narrative 输出出现大量空白或不完整 JSON。
换用原生 schema 后也不能假定所有 provider 的约束解码行为相同。旧 ReAct 工具同时遇到新增任意字典字段不适配 strict schema。

**修复与验证：** 保留 Pydantic 最终校验；支持显式 inline schema，`NarrativePlan` 默认使用
prompt JSON 后严格校验，格式失败只修本节点一次并计入预算。ReAct 工具使用旧字段投影保持兼容，
没有放开权限契约。[恢复测试](tests/test_planner_recovery.py) 覆盖原生解码失败、局部格式修复和坏依赖图。

**原理与预防：** API 的输出约束和应用的输入验证是两层保证；provider 能力应实测。
**面试表达：** 我没有把关闭严格校验当作兼容方案，而是调整生成方式并保留同一运行时验证边界。

## NDV2-04：过度保守的 token 预留把有效回应提前截断

**现象与根因：** 用 UTF-8 字节数预留中文 token、重复计算 prompt 中的 schema、给短输出一律预留
4096 token，会在最终台词或审核前错误耗尽额度。增加总预算只能掩盖多余调用。

**修复与验证：** 引入 tokenizer 与余量；词表不可用时才退回字节上界。各类节点使用一致的
实际输出上限与预留值，prompt JSON 不重复计算 schema；同时删除重复上下文和不必要的协作调用。
总预算保持 96,000 token。[传输回归](tests/test_model_transport_regressions.py) 验证预留与兜底。

**原理与预防：** 准入预估和实际计费必须区分，未知 usage 不能按零成本处理。
**面试表达：** 我同时修正计量误差和调用浪费，没有直接扩大预算换取通过率。

## NDV2-05：把检索命中全部写成台词证据

**现象与根因：** 无关的静态 Lore 命中也进入引用；另一方面，已送达 NPC 报告不在本拍 Lore 命中里，
又被误认为没有依据。把“搜到”“实际使用”“角色听说”混在一起导致误判与反复查证。

**修复与验证：** Writer 声明实际使用的 `used_lore_refs`、`used_fact_refs`；代码核对来源。
全部检索记录留在 trace，未使用的命中不进入演出证据；带来源的角色说法可转述，但不能自动升级为世界真相。
[回归测试](tests/test_episode_regressions.py) 覆盖无关命中不被引用。

**原理与预防：** 检索可用性、证据来源和认知可信度是不同维度。
**面试表达：** 我把证据的检索、引用和传播拆开，防止引用看似齐全但实际答非所问。

## NDV2-06：质量审核在错误的阶段要求整个故事已完成

**现象与根因：** Judge 曾要求提问这一拍先得到其他 NPC 的答案，或因内容尚未发布而否定已审核的邀请；
轻微措辞建议也可能导致整段回应被通用拒答替换。旧 `critical_choice` 标签还会机械触发人工审批。

**修复与验证：** 提供 `beat_contract`，明确本拍职责、延后事项和“审核后、投递前”的阶段；
非阻断风格建议不触发降级，只修问题所属节点及下游。新链在质量与冻结权限均通过后，不再仅凭意图标签拦截。
未放宽场景评测的平均 4/5 门槛。[单拍回归](tests/test_episode_regressions.py)、
[协作回归](tests/test_episode_coordination.py) 覆盖这一区别。

**原理与预防：** 局部后置条件不能要求未来事件已发生；审核标准应与当前生命周期阶段一致。
**面试表达：** 我把“这拍能否发出”和“整段任务是否成功”分别评判，避免互相阻塞。

## NDV2-07：把“准备咨询”当成“已经收到答复”

**现象与根因：** `consult_npc` 返回一个消息请求后，依赖它的谈判或规划就继续执行，
模型只能猜测他人条件，还浪费调用额度。多个目标不等于每拍都需要所有 Specialist。

**修复与验证：** 将依赖真实回复的操作放入 `ExecutionPlan.deferred_nodes`；完成投递后才启动对方，
回复再驱动后续规划。明确陈述自身条件的 NPC 回复可走短路径。
[协作测试](tests/test_episode_coordination.py) 断言咨询依赖节点不会提前执行。

**原理与预防：** 异步消息系统中，创建请求、交付请求和收到结果是三件事。
**面试表达：** 我让代码管理事件依赖，模型只提出计划，不能把预期结果当作既成事实。

## NDV2-08：去掉重复任务，却没把信息真正送给等待发言的人

**现象与根因：** 已排队角色可能听不到上一名角色的回复，继续重复询问；中间角色转问第三人后，
即使协调者已排队汇总，也会再被唤起。只按文本或单个请求去重不足以结束这种循环。

**修复与验证：** 合并已排队角色的询问；已加入交谈的角色在完成事件后共享可听见的发言，
名册中未加入的人仍不知情。协调者已等候汇总时，保留知识送达而不再生成中间角色的多余一拍；
`pending` 调查回复不触发无信息的重算。[协作测试](tests/test_episode_coordination.py) 覆盖三方顺序和私有隔离。

**原理与预防：** 去重不能丢失消息语义；交谈参与者与可选参与者必须分开。
**面试表达：** 我同时修复了重复调度和信息遗漏，使协作靠新信息推进，而不是靠反复调用推进。

## NDV2-09：为防私有信息泄漏，把审核返修意见删得过于笼统

**现象与根因：** Author 只收到“缩小范围、尊重事实”，无法修复“对话不等于物品交付”等具体问题，
两次候选重复同一错误。直接转发 Reviewer 的全文又可能泄漏其私有硬设定。

**修复与验证：** `revision_edits` 仅允许固定编辑代码和指向现有候选的合法字段路径；
Runtime 结合 Author 可见的 policy 生成具体反馈，不转发私密理由或隐藏答案。返修仍至多一次，并重新审核。
[原始失败 fixture](tests/fixtures/content_revision_failures.json) 与
[回归](tests/test_content_revision_feedback.py) 同时检查可修复性和不泄密。

**原理与预防：** 反馈通道既要最小授权，也要保留足够的纠错信息。
**面试表达：** 我把自然语言审校意见编译成受限编辑指令，解决了安全与可修复性之间的矛盾。

## NDV2-10：候选引用、正式 ID 与发布权限混用

**现象与根因：** 新任务内部步骤尚无正式 ID，可能被当作失效父目标；审核后的候选与最终台词所依据的
权限也需要一致。仅在完成时临时拼权限，会削弱“审核的是哪份内容”的确定性。

**修复与验证：** 只将新候选自身的临时引用编译进它的命名空间，不改写既有任务引用；
Runtime 分配正式 ID，编译精确状态路径，并在 Writer 前冻结摘要。完成事务只能验证既有策略，不能扩权。
[规划测试](tests/test_dynamic_planning.py) 检查 Writer 调用前策略已冻结；[内容测试](tests/test_content_review.py) 检查引用与权限。

**原理与预防：** 这是候选编译与事务准入问题，需要稳定身份和不可变授权快照。
**面试表达：** 我把模型建议先编译为明确 ID 和最小权限，再允许它生成最终呈现。

## NDV2-11：取消了 episode，但 outbox 和额度预留仍然活着

**现象与根因：** outbox 重发曾绕过取消检查；玩家抢占的个别路径只取消调度，没有清理未发出的暂存内容，
导致旧指令仍可能送出，或同目标永久被预留占用。

**修复与验证：** 统一取消入口，检查 outbox 对应 turn/episode，关闭失效消息并释放未发出内容的预留。
已经实际发出的演出仍等真实完成或中断，不回滚已发生行为。
[取消回归](tests/test_episode_regressions.py) 复现并验证了这两个问题。

**原理与预防：** 分布式状态取消需要同时覆盖任务、消息与资源占用；不能把一个状态字段当作完整取消。
**面试表达：** 我按生命周期检查所有副作用出口，保证取消后既不误发，也不遗留锁住目标的资源。

## NDV2-12：把新内容摘要作为 verified 知识

**现象与根因：** 摘要是未分类的叙事文字，可能描述信念或谎言；一律标为已核实，会让它成为兑现承诺的假证据。
临时内容还曾只在检索时过滤到期，失效暂存项仍占着去重位置。

**修复与验证：** 摘要按 `reported` 保存，只有显式审核的 `world_fact` 才进入事实层；
保留说话者、可分享性和到期时间，拒绝无时区日期，在发布时再次核验到期，清理失效暂存项。
[生命周期回归](tests/test_content_lifecycle.py) 验证摘要不能证明承诺完成、到期发布原子回滚和额度复用。

**原理与预防：** 被告知某事不等于证实某事；信息有效期也属于事实契约。
**面试表达：** 我把世界事实、角色认知和角色说法分层，避免记忆系统把一句话洗成世界真相。

## NDV2-13：用 NPC 的整体状态版本代表任务版本

**现象与根因：** NPC 的关系变化会推进自己的 domain version，不同 NPC 又有独立的时钟。
拿这个数字登记同一任务，真实评测出现 `cannot register a stale objective version`。

**修复与验证：** 既有任务引用从目标注册表读取；初始静态定义用版本 0，生成任务保留自己的生命周期版本。
旧快照仍不可覆盖较新定义。[版本测试](tests/test_objective_versions.py) 与
[协作测试](tests/test_episode_coordination.py) 覆盖独立时钟；三次真实复测版本错误为零，整体 2/3，另一例是预算降级。

**原理与预防：** 乐观锁的版本必须属于被保护的实体，不能拿另一个聚合根的时钟代替。
**面试表达：** 我把实体身份与版本所有权对齐，而不是简单允许旧版本覆盖来消除报错。

## NDV2-14：记忆按重要度取样，且丢失真实来源回合

**现象与根因：** 取前 12 条高重要度记忆不能找到更早但相关的扳手信息；摘要和原文重复注入还增加了预算。
蒸馏输入使用字符串，又把真实 turn ID 退化成 `history:N`。

**修复与验证：** 在实例/NPC 隔离后的最多 256 条候选上做 BM25，按输入和话题检索，
过滤最近原文重复项后最多注入四条，保留结构化来源；蒸馏从 `DialogueEvent` 保留真实 turn ID。
[相关性回归](tests/test_memory_relevance.py) 验证旧相关记忆胜过新无关记忆；协作测试验证来源没有退化。

**原理与预防：** 记忆的可用性取决于检索相关性与来源，不能仅取最近或最重要的片段。
**面试表达：** 我同时解决了“记得很多却找不到”和“能说出内容却无法追溯来源”的问题。

## NDV2-15：任务步骤只存在 trace，没有进入可检索的父任务状态

**现象与根因：** 结构化 `ObjectiveStep/ObjectiveEvent` 已出现在 trace，测试能看到它们，
但超过最近 12 条对话或重启后，规划器不能直接取回父任务中的路线定义。

**修复与验证：** 新增版本化的父任务定义存储，在完成事务里按实例、NPC 和父目标写入，
保留来源与重放账本；读取校验当前目标版本。它保存计划定义，不代表动作完成，也不占新支线额度。
[存储回归](tests/test_objective_plan_store.py)、[完整生命周期回归](tests/test_parent_plan_lifecycle.py)
和最终 27 次内容评测验证了重启、窗口外读取、中断与重复回执。

**原理与预防：** 可观测日志不能替代领域状态；未来决策需要可检索的结构化状态。
**面试表达：** 我把审计材料和真正供后续规划读取的任务状态分开，并让二者与完成事务一致。

## NDV2-16：评测素材和期望也会制造假失败

**现象与根因：** 曾要求直接拒绝复仇也必须先调用 Author；去重用例只说“旧信是独立支线”，
却不给独立目标；“复用事件”未明确事件已存在。另有把初始未接受状态当作永久约束、把新玩家回合当重复回执的误判。

**修复与验证：** 校准输入素材与期望，保留 [校准前快照](eval/reports/director_v2/cases-before-final-calibration.jsonl)；
保留普通想法与正式委托的对照场景。Reviewer 和 Planner 进一步区分讨论做法与正式发出委托，
最终三个 `casual_suggestion` 重复均没有创建任务。没有为了过门槛编造障碍、改动人物动机或抹掉旧失败。

**原理与预防：** 测试必须给被测系统足够的信息，且不能用内部调用次数代替外部行为目标。
**面试表达：** 我既修被测系统，也审查测试本身；修正口径时保留原始结果，不把两套数据当成严格对照。

## NDV2-17：把模型评审均分说得过于精确

**现象与根因：** 4.59/4.91 容易被理解为人工标定或玩家满意度；但本次是同型号模型的独立评审调用，
没有逐分行为锚点，也没有当前 episode 的人工评分样本。旧的十条二元校准不能为新指标背书。

**修复与验证：** 单独公开 [评分标准](EVALUATION_STANDARDS.md)，说明输入、评分单位、
两层门槛、失败样本的处理以及未做的标定；引用真实高低分例子，不事后更换尺度重写已有均分。

**原理与预防：** 测量有效性与程序正确性同样重要；同模型评审具有相关偏差。
**面试表达：** 我把结果能支持的结论和不能支持的结论一起写清，避免让小数位制造虚假的确定性。

## 当前仍未消除的质量问题

完整 live 为 97/102，最后九类内容回归为 26/27；它们达到约定门槛，并非所有输入都成功。
仍有未知事实只坦白却未给查证途径、长协作预算降级、对巡逻记录理解不稳，以及一次合并任务把
“征询接收意愿”说成“实际交付”的语义偏差。后者没有重复注册或重复奖励，仍被独立场景评审判失败。
详细指标、报告和最终代码对应关系见 [验收记录](VALIDATION_DIRECTOR_V2.md)。
