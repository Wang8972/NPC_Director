# NPC Director 商业化模型评测

本报告比较 `qwen3.8-flash`、`gpt-5.6-luna` 与已有 `gpt-5.6-sol` 基线，重点关注
质量、调用量和 token。共享 API 的等待、限流退避、连接错误和超时会污染墙钟时间，
因此**不使用本轮观测延迟推断正式环境延迟**。生产延迟需要在独享配额或目标部署环境重测。

## 结论

- `qwen3.8-flash` 在当前 idealab Responses API + Agents SDK 单轮结构化协议下不能直接替换：
  两个试点场景均失败，出现 Timeout 和 `MaxTurnsExceeded`；把节点超时从 60 秒放宽到 180 秒
  后仍是 0/2，说明问题不只是等待时间。
- `gpt-5.6-luna` 完成 34 场景 ×3 的全量评测，成功 **72/102（70.59%）**，未达到 90% 门槛。
  自然度 4.10、人设 4.51；20 次运行发生 fallback。
- Luna 的已回报生成 token 比 Sol 少约 7.8%，但 Luna 有 50 次调用未返回 usage，实际消耗
  被低估；按保守预算扣费的生成 token 反而比 Sol 高约 1.6%。主要成本优势来自模型单价，
  不是架构调用次数或 token 数显著减少。
- 当前不建议让 Luna 独立承担全部在线链路。更可行的商业化路径是：Luna 处理简单交流、
  事实边界、状态确认和既有任务步骤；复杂多 NPC 协商与新内容创作改为确定性流程、模板化
  或离线审核。Sol 只保留为可选质量上限，不作为默认在线模型。

## 对比数据

| 指标 | Qwen Flash 试点 | Luna 全量 | Sol 完整基线 |
|---|---:|---:|---:|
| 场景运行 | 2 | 102 | 102 |
| 成功 | 0/2 | 72/102（70.59%） | 97/102（95.10%） |
| 结构通过 | 0/2 | 76/102（74.51%） | 99/102（97.06%） |
| fallback 运行 | 2 | 20 | 1 |
| 自然度均值 | 1.50 | 4.10 | 4.59 |
| 人设均值 | 2.50 | 4.51 | 4.91 |
| 已回报生成 token | 24,050 | 4,329,804 | 4,694,262 |
| 已回报 Judge token | 6,612 | 222,529 | 237,975 |
| 生成调用 | 11 | 1,209 | 1,205 |
| 未返回 usage 的调用 | 7 | 50 | 2 |
| 保守预算扣减 token | 158,915 | 4,786,194 | 4,712,988 |
| 商业环境延迟 | 未测得 | 未测得 | 未测得 |

Qwen 的两次试点与 102 次全量结果样本量不同，只用于兼容性诊断，不能用于质量均值对比。
仓库没有配置三种模型的正式结算单价，因此不虚构金额。实际成本可按以下公式换算：

```text
成本 = 输入 token / 1,000,000 × 输入单价
     + 输出 token / 1,000,000 × 输出单价
```

完整 Luna 的已回报输入/输出分别为 4,054,216 / 275,588 token；Judge 已回报 222,529 token。
由于 50 次失败调用没有 usage，商业预算应优先采用保守扣减数或供应商账单，不应把未知调用按零计费。

## Luna 的适用范围

以下场景在三次重复中全部通过：指代跟进、玩家改口、重复输入、晚到 ACK、重启恢复、实例隔离、
私有知识、未知事实、拒绝不重复、讽刺问候、符合动机的隐瞒与谎言、近距离行动、远距离父任务步骤、
既有分支、既有问路步骤、合并已有任务、拒绝无动机支线及中断内容。

明显不稳定的场景：

| 场景 | Luna 通过 | 主要问题 |
|---|---:|---|
| 复合请求 | 0/3 | 未注册父任务动作、后续回合缺失或 fallback |
| 三 NPC 协商 | 0/3 | 条件整合和预算控制失败 |
| 任务反提议 | 0/3 | 协商结构或状态提交失败 |
| 世界事件 | 0/3 | 重规划/状态路径导致 fallback |
| 新增任务内事件 | 0/3 | Author/Reviewer 链不稳定 |
| 独立短支线 | 1/3 | 内容生成或审核失败 |
| NPC 咨询 | 1/3 | 多角色后继调度 fallback |

这表明 Luna 的主要短板不是一般台词，而是跨角色依赖、复杂结构化规划和新内容审核。

## Qwen Flash 兼容性诊断

第一次试点使用 60 秒节点超时：两个场景均失败。成功的 Planner 调用观测为约 40–64 秒，
另有多次 60 秒超时；这些数字受共享 API 影响，不作为商业延迟指标。

第二次将节点超时放宽到 180 秒，仍为 0/2。失败包含 `MaxTurnsExceeded`：模型在当前
单轮结构化调用内没有形成最终结果。临时允许两轮调用的诊断在用户要求切换 Luna 后停止，
未形成报告；相关临时代码已全部回退。

在重新评估 Qwen 前，需要先在目标生产端点确认：

1. Responses API 对该模型的结构化 JSON 支持方式；
2. 是否需要多轮 continuation 才会输出最终结果；
3. 独享配额下的服务端处理时间和吞吐；
4. 失败调用是否计费以及真实结算单价。

## 商业化建议

目前可以把 Luna 作为低成本在线候选，但需要收窄职责：

- 允许：简单交流、单 NPC 事实问答、确认/拒绝、既有任务步骤、幂等与状态反馈；
- 优先确定性实现：多方发言顺序、条件合并、世界事件优先级、任务状态推进；
- 离线或人工审核：新支线、新背景事实及复杂遭遇事件；
- 禁止无条件自动升级到高价模型；若将来启用质量升级，必须设置按场景分类的调用上限和成本预算。

这不是建议直接上线当前 Luna 全链路。它没有达到质量门槛；下一步应减少简单场景的模型节点数，
把复杂协作从自由生成改成结构化状态机，再对 Luna 重新跑同一套 102 次评测。

## 证据与可复现性

- Luna 原始完整报告：`artifacts/director-v2/episodes-live-luna-commercial-34x3.json`
- Qwen 60 秒试点：`artifacts/director-v2/qwen38-flash-pilot.json`
- Qwen 180 秒试点：`artifacts/director-v2/qwen38-flash-pilot-180s.json`
- Sol 对照报告：[版本化摘要](eval/reports/director_v2/episodes-live-sol-release-34x3.json)
- 评分标准：[EVALUATION_STANDARDS.md](EVALUATION_STANDARDS.md)

Qwen/Luna 测试的所有生成角色、Judge 和 fallback 都固定为对应模型。测试结束后，统一模型参数和
两轮 Qwen 诊断改动已经回退；`git diff` 确认没有留下测试代码修改。
