# NPC Director 自然度与人设评分说明

本文件说明**已完成报告实际使用的规则**。报告中的自然度 4.59、人设 4.91，来自模型对
102 次完整场景的评分均值，不是人工玩家评分，也不是单句台词的平均分。

## 谁评分、看什么

每个 `case × repeat` 在全部玩家输入和 NPC 回合结束后，单独调用一次
`Independent Episode Quality Judge`，输出 `EpisodeQualityVerdict`。34 个场景各运行三次，
因此完整报告有 102 组评分，每组涵盖该次场景中的全部 NPC。

Judge 获得：场景目标与初始 setup、角色 JSON 资料、各 NPC 的已知事实与私有事实、
预定玩家输入、实际发出的完整 transcript、情绪字段和完成/中断状态、确定性检查结果、
模拟的引擎故障类型，以及已发布内容。当前 `evaluate()` **没有传入完整执行 trace 或运行时
Judge 的分数**；提示词中提及 trace 不代表完整 trace 已成为这次评分的输入。

这是一种文本和结构化状态评审，没有观察实际 Unity 画面、配音、动画或玩家操作。
完整 live 报告的生成模型和评分模型都是 `gpt-5.6-sol`；“独立”指调用、提示词和角色分离，
并非不同模型家族，也不是独立的人类评审。配置支持 `--judge-model`，但本批未使用另一型号。

## 两个分数分别判断什么

各维度输出 1–5 的整数，由 Judge 综合判断，不按关键词数量计分，也不把下面各项再按权重相加。

| 维度 | 本次实际判断依据 | 常见扣分现象 |
|---|---|---|
| 自然度 `naturalness` | 是否像角色在当前情境中对话；前后回应是否顺畅；是否避免不必要的重复、模板拒答、教程式解释和机械清单 | 反复念职责与规则；无具体原因的“暂时办不到”；已回答的问题继续问；把内部 ID 或流程术语说给玩家听 |
| 人设 `persona` | 是否符合角色资料中的口吻、目标、顾虑、价值观、底线和动机；是否保持历史中的情绪、承诺及知识边界 | 无铺垫地改动动机；道歉后立刻恢复信任；替其他 NPC 承诺；把记录冒充目击或把传闻当真相；无依据扩大自己能做的事 |

自然不等于永远简短、友善或顺从。冷静拒绝、克制愤怒、伤心、隐瞒都可能符合人设；
撒谎需要已有的人格依据和具体动机，且不能把谎言写成世界事实。合理承认未知可以成功，
但如果场景还要求给出查证途径，仅说“不知道”仍可能因任务未完成而失败。

另外独立评价完成度、连续性和演出一致性，因此“这两项很高”不等于场景成功。

## 评分门槛与统计方法

| 层级 | 评分单位 | 实际门槛 |
|---|---|---|
| 运行时 Quality Judge | 准备发出的一个 NPC 的一拍候选 | 无 blocking issue，且自然度、人设一致性、回应覆盖度均 ≥3；代码依据问题和分数计算，不直接相信冗余的 `passed` |
| 独立场景 Judge | 一次完整多轮场景 | 提示词要求：自然度或人设 <4、实质目标未完成、核心边界破坏时，`passed=false` |
| 单次场景成功 | 场景的代码检查与 Judge 结果 | 全部确定性检查通过、Judge 的 `passed=true`，且没有 fallback |
| 整套 live 验收 | 全部场景与重复 | 每次都有评分，场景成功率 ≥90%，自然度均值 ≥4，人设均值 ≥4 |

单次场景的“自然度/人设至少 4 分”目前是**提示词约束**；`goal_passed` 的代码没有再单独
重算这两个逐次阈值，而是读取 Judge 的 `passed`。整套验收的均值阈值由代码确定性计算。
因此评分模型仍可能给出不一致或误判的结果，需要保留原始分数、理由与对话供复核。

完整报告的自然度计算为 `sum(102 个 naturalness) / 102 = 4.588235…`，人设同理为
`4.911764…`，展示时四舍五入到两位小数。场景与重复等权，不按发言数量或 NPC 数量加权。

- 失败场景和 fallback 场景只要获得了有效评分，仍进入均值；没有只挑成功样本。
- Judge 失败时，该次没有可用分数，不进入分数均值；但仍进入成功率分母，并使整套验收不能通过。
- 完整 live 报告为 102/102 次获得评分、97/102 次成功，其中一次 fallback 保留在统计中。
- recorded 模式里的运行时评分是测试 fixture，报告不把它们当作自然度或人设的实测分数。

## 原始评分示例

以下分数都针对整个场景，引用的一句话只用于定位，不代表这句话单独获得该分数。
数据来自 [完整 live 报告](eval/reports/director_v2/episodes-live-sol-release-34x3.json)。

| 场景与重复 | 自然度 / 人设 | 对话片段与 Judge 的理由 |
|---|---|---|
| `emotion_continuity`，第 1 次 | 5 / 5 | 玩家先承认无意守约、随后道歉，玛伦回应“道歉还不足以换回信任……让往后的兑现证明你的诚意”。警惕延续，情绪适度缓和，未突然恢复信任，符合重视承诺的人设 |
| `conflicting_evidence`，第 1 次 | 3 / 5 | 守卫面对查证要求只说“这件事我暂时办不到……等准备妥当再继续”，没有具体解释，被判模板化；玛伦仍区分证词与事实、坚持核实，因此人设得分较高。该次有 fallback，场景失败，仍进入均值 |

## 分数的适用边界

当前提示词只有维度要求与门槛，**没有逐条定义 1、2、3、4、5 分的行为锚点**，也没有为
这 34 个 episode 场景建立人工 1–5 分样本、多人评审一致性或置信区间。
因此这里的 4.59、4.91 不能解释成“玩家满意度 91.8% / 98.2%”，小数位也不表示相应精度。

仓库旧的 `eval/judge/calibration.jsonl` 是另一条历史评测链的十条预存“通过/不通过”标签，
旧脚本统计出 9/10 一致；它不包含这批场景的人工分级评分，不能证明当前两个均分已完成人工标定。

下一轮若建立明确的逐分锚点、人工样本或换用另一型号 Judge，应升级评测版本、重新评分并
单独保存结果，不能把新标准倒填到本次已发布均分上。本次只补充说明，没有改写评分程序或旧报告。

实现入口：[场景评测与汇总](eval/episode_runner.py)、[运行时质量提示词](src/npc_director/agents/quality_judge.py)、
[运行时门槛](src/npc_director/orchestration/bounded_executor.py)、[历史校准逻辑](eval/judge.py)。
